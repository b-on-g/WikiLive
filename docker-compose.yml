version: "3.9"

services:
  wikilive:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9082:9080"
    restart: unless-stopped
    tty: true
    stdin_open: true
    command: "npm start"

    # http://localhost:9082/bog/WikiLive/app/-/test.html
